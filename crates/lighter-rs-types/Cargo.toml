[package]
name = "lighter-rs-types"
version = "0.1.0"
edition = "2021"
description = "Shared Rust types for Lighter (Robinhood Chain / zkLighter) exchange wire formats"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
