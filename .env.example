# The real TimescaleDB instance runs on the VM host, not in this repo's
# docker-compose db service -- see CLAUDE.md, "Where the TimescaleDB
# instance actually lives".
export DATABASE_URL=postgresql://postgres@[host address removed]/market_data
export LIGHTER_API_URL=https://api.rh.lighter.xyz
export LIGHTER_WS_URL=wss://api.rh.lighter.xyz/stream?readonly=true
